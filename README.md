# SectorScout

SectorScout is being implemented in phases. Phase 0 contains only project
foundation work: configuration loading, reproducibility metadata, NYSE market
calendar handling, a minimal DuckDB schema, CLI placeholders, and tests.

Phase 0 intentionally does not implement indicators, scoring, setup detection,
backtesting, dashboards, or live data providers.

