[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sectorscout"
version = "0.1.0"
description = "Point-in-time sector and setup research system built in staged phases."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "duckdb>=1.1",
  "exchange-calendars>=4.5,<4.6",
  "numpy>=1.26,<2",
  "pandas>=2.2,<3",
  "pydantic>=2.7",
  "protobuf>=4.25,<5",
  "pytz>=2024.1",
  "pyyaml>=6.0",
  "streamlit>=1.35,<1.36",
  "typer>=0.12",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.2",
]

[project.scripts]
sectorscout = "sectorscout.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
sectorscout = ["schema.sql", "intel/fixtures/*.md"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
